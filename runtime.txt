{% extends 'base.html' %}

{% block content %}
<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-indigo-200 via-blue-100 to-purple-200 py-10 px-4">
  <div class="w-full max-w-5xl bg-white/80 backdrop-blur-md rounded-3xl shadow-2xl border border-indigo-100 p-6 md:p-10 animate-fade-in">
    <!-- Header Section -->
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6 gap-3">
        <div>
            <h2 class="text-2xl md:text-3xl font-bold text-gray-800">Packing List Details</h2>
            <p class="text-gray-600 text-sm md:text-base mt-1">Order: {{ production_order_code }} | Demand: {{ production_demand_code }}</p>
            <p class="text-gray-600 text-sm md:text-base">Pallet: {{ pallet_number }} | Box: {{ box_number }}</p>
        </div>
        <a href="{% url 'fetch_data' %}" class="flex items-center bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-3 md:px-4 rounded-lg transition-all w-full md:w-auto justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
            </svg>
            Back to Scan
        </a>
    </div>

    <!-- Customer Card - Mobile Optimized -->
    <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl p-4 md:p-6 mb-6 border border-blue-100">
        <h3 class="text-lg md:text-xl font-semibold text-gray-800 mb-3 md:mb-4 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 md:h-6 md:w-6 mr-2 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
            Customer Information
        </h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 md:gap-4">
            <div class="bg-white p-3 md:p-4 rounded-lg shadow-sm">
                <p class="text-xs md:text-sm text-gray-500">Customer Name</p>
                <p class="font-medium text-gray-800 text-sm md:text-base">{{ customer_data.CustomerName|default:"-" }}</p>
            </div>
            <div class="bg-white p-3 md:p-4 rounded-lg shadow-sm">
                <p class="text-xs md:text-sm text-gray-500">PO Number</p>
                <p class="font-medium text-gray-800 text-sm md:text-base">{{ customer_data.CustomerPO|default:"-" }}</p>
            </div>
            <div class="bg-white p-3 md:p-4 rounded-lg shadow-sm">
                <p class="text-xs md:text-sm text-gray-500">Customer Code</p>
                <p class="font-medium text-gray-800 text-sm md:text-base">{{ customer_data.CustomerCode|default:"-" }}</p>
            </div>
        </div>
    </div>

    <!-- Kit Elements Section - Optimized for Mobile -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
        <div class="px-4 md:px-6 py-3 md:py-4 border-b border-gray-200 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-2">
            <h3 class="text-lg md:text-xl font-semibold text-gray-800 flex items-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 md:h-6 md:w-6 mr-2 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
                </svg>
                Kit Elements
            </h3>
            <span class="bg-blue-100 text-blue-800 text-xs md:text-sm font-medium px-2 md:px-3 py-1 rounded-full">
                {{ kit_elements|length }} items
            </span>
        </div>

        <!-- Mobile Cards View -->
        <div class="block sm:hidden divide-y divide-gray-200 max-h-[calc(100vh-300px)] overflow-y-auto">
            {% for element in kit_elements %}
            <div class="p-4 {% if element.is_verified %}bg-green-100 border-l-4 border-green-400{% elif element.image_url %}bg-green-50{% endif %}" data-element-id="{{ element.id }}">
                <div class="flex justify-between items-start mb-2">
                    <div>
                        <span class="px-2 py-1 bg-blue-100 text-blue-800 rounded-full text-xs font-medium mr-2">
                            Pallet: {{ element.PALLETNUMBER }}
                        </span>
                        <span class="px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs font-medium">
                            Box: {{ element.BOXSEQUENCE }}
                        </span>
                    </div>
                    <div class="flex items-center space-x-1">
                        {% if element.image_url %}
                        <!-- Show checkmark when image exists -->
                        <span title="Image uploaded" class="p-1.5">
                            <svg class="w-4 h-4 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                            </svg>
                        </span>
                        <!-- Remove Image Button -->
                        <button onclick="deleteElementImage('{{ element.image_url|escapejs }}', '{{ element.id }}')" 
                                class="p-2 bg-red-100 hover:bg-red-200 rounded-lg transition-colors delete-btn">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                            </svg>
                        </button>
                        {% else %}
                        <!-- Camera Capture Button -->
                        <button onclick="openCameraModal('{{ element.id }}', '{{ element.ELEMENTDESC|escapejs }}')" 
                                class="p-1.5 bg-blue-100 hover:bg-blue-200 rounded-lg transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </button>
                        {% endif %}
                    </div>
                </div>
                
                <p class="text-gray-800 text-sm mb-3 {% if element.is_verified %}line-through text-green-600{% endif %}">
                    {{ element.ELEMENTDESC }}
                </p>
                
                <div class="flex items-center space-x-3">
                    <div class="relative">
                        {% if element.image_url %}
                            <img src="{{ element.image_url }}"
                                alt="{{ element.ELEMENTDESC|truncatechars:30 }}"
                                class="w-14 h-14 object-cover rounded-lg border border-gray-200 cursor-pointer element-image"
                                onclick="showImageModal('{{ element.image_url|escapejs }}', '{{ element.ELEMENTDESC|escapejs }}')"
                                onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                            <div class="w-14 h-14 bg-gray-100 rounded-lg border border-dashed border-gray-300 items-center justify-center hidden">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="text-gray-400" viewBox="0 0 16 16">
                                    <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                    <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-12zm12 1a1 1 0 0 1 1 1v6.5l-3.777-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12V3a1 1 0 0 1 1-1h12z"/>
                                </svg>
                            </div>
                        {% else %}
                            <div class="w-14 h-14 bg-gray-100 rounded-lg border border-dashed border-gray-300 flex items-center justify-center">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="text-gray-400" viewBox="0 0 16 16">
                                    <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                    <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-12zm12 1a1 1 0 0 1 1 1v6.5l-3.777-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12V3a1 1 0 0 1 1-1h12z"/>
                                </svg>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% empty %}
            <div class="p-6 text-center text-gray-500">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 mx-auto text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <p class="mt-2 text-gray-600 text-sm">No kit elements found</p>
            </div>
            {% endfor %}
        </div>

        <!-- Desktop Table View -->
        <div class="hidden sm:block overflow-x-auto">
            <div class="max-h-[calc(100vh-300px)] md:max-h-[calc(100vh-350px)] overflow-y-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50 sticky top-0">
                        <tr>
                            <th scope="col" class="px-4 md:px-6 py-3 text-left text-xs md:text-sm font-medium text-gray-500 uppercase tracking-wider">Pallet</th>
                            <th scope="col" class="px-4 md:px-6 py-3 text-left text-xs md:text-sm font-medium text-gray-500 uppercase tracking-wider">Box</th>
                            <th scope="col" class="px-4 md:px-6 py-3 text-left text-xs md:text-sm font-medium text-gray-500 uppercase tracking-wider">Description</th>
                            <th scope="col" class="px-4 md:px-6 py-3 text-left text-xs md:text-sm font-medium text-gray-500 uppercase tracking-wider">Image</th>
                            <th scope="col" class="px-4 md:px-6 py-3 text-left text-xs md:text-sm font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        {% for element in kit_elements %}
                        <tr class="{% if element.is_verified %}bg-green-100 border-l-4 border-green-400{% elif element.image_url %}bg-green-50{% endif %} hover:bg-gray-50 transition-colors" data-element-id="{{ element.id }}">
                            <td class="px-4 md:px-6 py-4 whitespace-nowrap">
                                <span class="px-2 py-1 bg-blue-100 text-blue-800 rounded-full text-xs md:text-sm font-medium">
                                    {{ element.PALLETNUMBER }}
                                </span>
                            </td>
                            <td class="px-4 md:px-6 py-4 whitespace-nowrap">
                                <span class="px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs md:text-sm font-medium">
                                    {{ element.BOXSEQUENCE }}
                                </span>
                            </td>
                            <td class="px-4 md:px-6 py-4">
                                <p class="text-gray-800 text-sm md:text-base {% if element.is_verified %}line-through text-green-600{% endif %}">
                                    {{ element.ELEMENTDESC }}
                                </p>
                            </td>
                            <td class="px-4 py-2 text-center image-cell">
                                {% if element.image_url %}
                                    <img src="{{ element.image_url }}"
                                        alt="{{ element.ELEMENTDESC }}"
                                        class="w-16 h-16 object-cover rounded-lg border border-gray-200 cursor-pointer mx-auto element-image"
                                        onclick="showImageModal('{{ element.image_url|escapejs }}', '{{ element.ELEMENTDESC|escapejs }}')"
                                        onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                    <div class="w-16 h-16 bg-gray-100 rounded-lg border border-dashed border-gray-300 flex items-center justify-center mx-auto hidden">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="text-gray-400" viewBox="0 0 16 16">
                                            <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                            <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-12zm12 1a1 1 0 0 1 1 1v6.5l-3.777-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12V3a1 1 0 0 1 1-1h12z"/>
                                        </svg>
                                    </div>
                                {% else %}
                                    <div class="w-16 h-16 bg-gray-100 rounded-lg border border-dashed border-gray-300 flex items-center justify-center mx-auto">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="text-gray-400" viewBox="0 0 16 16">
                                            <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                            <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-12zm12 1a1 1 0 0 1 1 1v6.5l-3.777-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12V3a1 1 0 0 1 1-1h12z"/>
                                        </svg>
                                    </div>
                                {% endif %}
                            </td>
                            <td class="px-4 md:px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center space-x-2">
                                    {% if element.image_url %}
                                    <!-- Show checkmark when image exists -->
                                    <span title="Image uploaded" class="p-2">
                                        <svg class="w-5 h-5 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                                        </svg>
                                    </span>
                                    <!-- Remove Image Button -->
                                    <button onclick="deleteElementImage('{{ element.image_url|escapejs }}', '{{ element.id }}')" 
                                            class="p-2 bg-red-100 hover:bg-red-200 rounded-lg transition-colors delete-btn">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                        </svg>
                                    </button>
                                    {% else %}
                                    <!-- Camera Capture Button -->
                                    <button onclick="openCameraModal('{{ element.id }}', '{{ element.ELEMENTDESC|escapejs }}')" 
                                            class="p-2 bg-blue-100 hover:bg-blue-200 rounded-lg transition-colors">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
                                        </svg>
                                    </button>
                                    {% endif %}
                                </div>
                            </td>
                        </tr>
                        {% empty %}
                        <tr>
                            <td colspan="5" class="px-6 py-4 text-center text-gray-500">
                                <div class="flex flex-col items-center justify-center py-8">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                    </svg>
                                    <p class="mt-2 text-gray-600">No kit elements found</p>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div> 
   
<!-- Image Modal -->
<div id="imageModal" class="fixed inset-0 z-50 hidden bg-black bg-opacity-75 p-4" onclick="closeImageModal(event)">
    <div class="flex items-center justify-center min-h-screen">
        <div class="bg-white rounded-lg w-full max-w-4xl max-h-[90vh] overflow-auto">
            <div class="flex justify-between items-center p-3 md:p-4 border-b sticky top-0 bg-white z-10">
                <h3 id="modalTitle" class="text-base md:text-lg font-semibold"></h3>
                <button onclick="closeImageModal()" class="text-gray-500 hover:text-gray-700">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <div class="p-3 md:p-4 flex justify-center items-center">
                <img id="modalImage" src="" alt="Full size image" class="max-w-full max-h-[70vh] object-contain rounded">
            </div>
        </div>
    </div>
</div>

<!-- Camera Capture Modal -->
<div id="cameraModal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black bg-opacity-75 p-4">
    <div class="bg-white rounded-lg w-full max-w-4xl max-h-[90vh] overflow-auto">
        <div class="flex justify-between items-center p-3 md:p-4 border-b sticky top-0 bg-white z-10">
            <h3 id="cameraModalTitle" class="text-base md:text-lg font-semibold">Take a Picture</h3>
            <button onclick="closeCameraModal()" class="text-gray-500 hover:text-gray-700">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
        <div class="p-3 md:p-4">
            <div class="flex flex-col items-center">
                <!-- Camera Container -->
                <div class="relative w-full max-w-3xl mx-auto">
                    <div class="camera-container bg-black rounded-lg overflow-hidden mb-4">
                        <video id="cameraFeed" autoplay playsinline class="w-full h-auto max-h-[60vh] object-contain"></video>
                    </div>
                    
                    <!-- Camera controls overlay -->
                    <div class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex space-x-4">
                        <button id="captureBtn" onclick="captureImage()" 
                                class="bg-white hover:bg-gray-100 text-gray-800 font-medium py-3 px-4 rounded-full transition-all flex items-center shadow-lg">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <circle cx="12" cy="12" r="10" stroke-width="2"/>
                                <circle cx="12" cy="12" r="3" stroke-width="2"/>
                            </svg>
                        </button>
                        
                        <button onclick="switchCamera()" 
                                class="bg-white hover:bg-gray-100 text-gray-800 font-medium py-3 px-4 rounded-full transition-all flex items-center shadow-lg">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
                            </svg>
                        </button>
                    </div>
                </div>
                
                <!-- Preview Container -->
                <div id="previewContainer" class="mt-4 hidden w-full max-w-3xl mx-auto">
                    <p class="text-sm text-gray-600 mb-2 text-center">Preview:</p>
                    <div class="bg-black rounded-lg overflow-hidden mb-4">
                        <canvas id="imagePreview" class="w-full h-auto max-h-[60vh] object-contain"></canvas>
                    </div>
                    <div class="flex justify-center space-x-4 mt-4">
                        <button type="button" onclick="uploadPicture()" class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-6 rounded-lg transition-all flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                            </svg>
                            Upload Image
                        </button>
                        <button type="button" onclick="retakePicture()" class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium py-2 px-6 rounded-lg transition-all flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                            </svg>
                            Retake
                        </button>
                    </div>
                </div>
                
                <!-- Hidden form for uploading captured images -->
                <form id="uploadForm" method="post" enctype="multipart/form-data" action="{% url 'upload_element_image' %}" style="display: none;">
                    {% csrf_token %}
                    <input type="hidden" id="elementIdInput" name="element_id">
                    <input type="hidden" id="elementDescInput" name="element_desc">
                    <input type="hidden" id="productionOrderInput" name="production_order_code" value="{{ production_order_code }}">
                    <input type="hidden" id="productionDemandInput" name="production_demand_code" value="{{ production_demand_code }}">
                    <input type="hidden" id="palletNumberInput" name="pallet_number" value="{{ pallet_number }}">
                    <input type="hidden" id="boxNumberInput" name="box_number" value="{{ box_number }}">
                    <input type="hidden" id="customerNameInput" name="customer_name" value="{{ customer_data.CustomerName }}">
                    <input type="hidden" id="customerPoInput" name="customer_po" value="{{ customer_data.CustomerPO }}">
                    <input type="hidden" id="imageDataInput" name="element_image">
                </form>
            </div>
        </div>
    </div>
</div>

<script>
// Global variables
let stream = null;
let currentElementId = null;
let currentFacingMode = 'environment'; // Start with rear camera
let capturedImageBlob = null;

// Image Modal Functions
function showImageModal(imageUrl, title) {
    if (!imageUrl) {
        console.log('No image URL provided');
        return;
    }
    
    // Set title
    document.getElementById('modalTitle').textContent = title || 'Image Preview';
    
    // Show loading state
    const modalImage = document.getElementById('modalImage');
    modalImage.src = '';
    modalImage.classList.add('opacity-0');
    
    // Test if image exists before showing modal
    const testImg = new Image();
    testImg.onload = function() {
        modalImage.src = imageUrl;
        modalImage.classList.remove('opacity-0');
        document.getElementById('imageModal').classList.remove('hidden');
        document.getElementById('imageModal').style.display = 'flex';
        document.body.style.overflow = 'hidden';
    };
    testImg.onerror = function() {
        alert('Image not found or failed to load');
        console.error('Failed to load image:', imageUrl);
    };
    testImg.src = imageUrl;
}

function closeImageModal(event) {
    // Close modal if clicking outside the content or on close button
    if (!event || event.target.id === 'imageModal' || event.target.closest('button')) {
        document.getElementById('imageModal').classList.add('hidden');
        document.getElementById('imageModal').style.display = 'none';
        document.body.style.overflow = 'auto';
        document.getElementById('modalImage').src = '';
    }
}

// Image Deletion Function
function deleteElementImage(imageUrl, elementId) {
    if (!imageUrl) {
        alert('No image URL provided for deletion');
        return;
    }

    if (!confirm('Are you sure you want to permanently delete this image?')) {
        return;
    }

    // Show loading state
    const deleteButton = document.querySelector(`button[onclick*="${imageUrl}"]`);
    if (deleteButton) {
        deleteButton.disabled = true;
        deleteButton.innerHTML = `
            <svg class="animate-spin h-5 w-5 text-red-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
        `;
    }

    fetch('/delete_element_image/', {
        method: 'POST',
        headers: {
            'X-CSRFToken': getCookie('csrftoken'),
            'Content-Type': 'application/x-www-form-urlencoded',
        },
        body: `image_url=${encodeURIComponent(imageUrl)}`
    })
    .then(response => {
        if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
        }
        return response.json();
    })
    .then(data => {
        if (data.status === 'success') {
            // Update the UI without full page reload
            const elementRow = document.querySelector(`[data-element-id="${elementId}"]`);
            if (elementRow) {
                // Remove green background
                elementRow.classList.remove('bg-green-50');
                elementRow.classList.remove('bg-green-100');
                
                // Update image cell
                const imageCell = elementRow.querySelector('.image-cell');
                if (imageCell) {
                    imageCell.innerHTML = `
                        <div class="w-16 h-16 bg-gray-100 rounded-lg border border-dashed border-gray-300 flex items-center justify-center mx-auto">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="text-gray-400" viewBox="0 0 16 16">
                                <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z"/>
                                <path d="M2.002 1a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2h-12zm12 1a1 1 0 0 1 1 1v6.5l-3.777-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12V3a1 1 0 0 1 1-1h12z"/>
                            </svg>
                        </div>
                    `;
                }
                
                // Update actions cell
                const actionsCell = elementRow.querySelector('.flex.items-center.space-x-2');
                if (actionsCell) {
                    actionsCell.innerHTML = `
                        <button onclick="openCameraModal('${elementId}', '${data.element_desc || ''}')" 
                                class="p-2 bg-blue-100 hover:bg-blue-200 rounded-lg transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </button>
                    `;
                }
            }
            
            // Show success message
            showToast('Image deleted successfully!', 'success');
        } else {
            throw new Error(data.message || 'Unknown error occurred');
        }
    })
    .catch(error => {
        console.error('Delete error:', error);
        showToast(`Error deleting image: ${error.message}`, 'error');
        
        // Reset button state
        if (deleteButton) {
            deleteButton.disabled = false;
            deleteButton.innerHTML = `
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                </svg>
            `;
        }
    });
}

// Camera Modal Functions
function openCameraModal(elementId, elementDesc) {
    currentElementId = elementId;
    document.getElementById('elementIdInput').value = elementId;
    document.getElementById('elementDescInput').value = elementDesc;
    document.getElementById('cameraModalTitle').textContent = `Take Picture: ${elementDesc}`;
    document.getElementById('cameraModal').classList.remove('hidden');
    document.getElementById('cameraModal').style.display = 'flex';
    document.body.style.overflow = 'hidden';
    startCamera();
}

function closeCameraModal() {
    stopCamera();
    document.getElementById('cameraModal').classList.add('hidden');
    document.getElementById('cameraModal').style.display = 'none';
    document.getElementById('previewContainer').classList.add('hidden');
    document.getElementById('cameraFeed').classList.remove('hidden');
    document.body.style.overflow = 'auto';
}

async function startCamera() {
    try {
        const constraints = {
            video: {
                facingMode: currentFacingMode,
                width: { ideal: 1920 },
                height: { ideal: 1080 }
            }
        };
        
        stream = await navigator.mediaDevices.getUserMedia(constraints);
        const videoElement = document.getElementById('cameraFeed');
        videoElement.srcObject = stream;
        
        // Wait for video to be ready
        videoElement.onloadedmetadata = () => {
            videoElement.play();
        };
    } catch (err) {
        console.error("Error accessing camera: ", err);
        alert("Could not access the camera. Please ensure you have granted camera permissions.");
        closeCameraModal();
    }
}

function stopCamera() {
    if (stream) {
        stream.getTracks().forEach(track => {
            track.stop();
        });
        stream = null;
    }
}

function switchCamera() {
    currentFacingMode = currentFacingMode === 'user' ? 'environment' : 'user';
    stopCamera();
    startCamera();
}

function captureImage() {
    const video = document.getElementById('cameraFeed');
    const canvas = document.getElementById('imagePreview');
    const context = canvas.getContext('2d');
    
    // Set canvas dimensions to match video's natural aspect ratio
    const videoAspectRatio = video.videoWidth / video.videoHeight;
    const maxWidth = 1920;
    const maxHeight = 1080;
    
    let width = video.videoWidth;
    let height = video.videoHeight;
    
    // Maintain aspect ratio while fitting within bounds
    if (width > maxWidth) {
        width = maxWidth;
        height = width / videoAspectRatio;
    }
    if (height > maxHeight) {
        height = maxHeight;
        width = height * videoAspectRatio;
    }
    
    canvas.width = width;
    canvas.height = height;
    
    // Draw current video frame to canvas
    context.drawImage(video, 0, 0, width, height);
    
    // Apply image enhancement (optional)
    // context.filter = 'contrast(1.1) brightness(1.1)';
    // context.drawImage(canvas, 0, 0);
    
    // Show preview and hide video feed
    document.getElementById('previewContainer').classList.remove('hidden');
    video.classList.add('hidden');
    
    // Convert canvas to Blob
    canvas.toBlob(function(blob) {
        capturedImageBlob = blob;
    }, 'image/jpeg', 0.9);
}

function retakePicture() {
    document.getElementById('previewContainer').classList.add('hidden');
    document.getElementById('cameraFeed').classList.remove('hidden');
    capturedImageBlob = null;
}

function uploadPicture() {
    // Get the canvas and convert to base64
    var canvas = document.getElementById('imagePreview');
    var dataUrl = canvas.toDataURL('image/jpeg', 0.9);
    
    // Set the base64 data (remove the prefix)
    document.getElementById('imageDataInput').value = dataUrl.split(',')[1];

    // Show loading state
    const uploadBtn = document.querySelector('button[onclick="uploadPicture()"]');
    const originalText = uploadBtn.innerHTML;
    uploadBtn.innerHTML = `
        <svg class="animate-spin h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        Uploading...
    `;
    uploadBtn.disabled = true;

    // Submit the form via AJAX
    var form = document.getElementById('uploadForm');
    var formData = new FormData(form);

    fetch(form.action, {
        method: 'POST',
        body: formData,
        headers: {
            'X-CSRFToken': getCookie('csrftoken')
        }
    })
    .then(response => {
        if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
        }
        return response.json();
    })
    .then(data => {
        if (data.status === 'success') {
            showToast('Image uploaded successfully!', 'success');
            // Reload after short delay
            setTimeout(() => {
                window.location.reload();
            }, 1000);
        } else {
            throw new Error(data.message || 'Unknown error');
        }
    })
    .catch(error => {
        console.error('Upload error:', error);
        showToast(`Error uploading image: ${error.message}`, 'error');
        
        // Reset button state
        uploadBtn.innerHTML = originalText;
        uploadBtn.disabled = false;
    });
}

// Toast notification function
function showToast(message, type = 'info') {
    // Remove existing toast if any
    const existingToast = document.getElementById('custom-toast');
    if (existingToast) {
        existingToast.remove();
    }
    
    const toast = document.createElement('div');
    toast.id = 'custom-toast';
    toast.className = `fixed top-4 right-4 z-50 px-6 py-3 rounded-lg shadow-lg text-white font-medium animate-fade-in ${
        type === 'success' ? 'bg-green-500' : 
        type === 'error' ? 'bg-red-500' : 
        'bg-blue-500'
    }`;
    toast.textContent = message;
    document.body.appendChild(toast);
    
    // Auto remove after 3 seconds
    setTimeout(() => {
        toast.classList.add('animate-fade-out');
        setTimeout(() => {
            if (toast.parentNode) {
                toast.parentNode.removeChild(toast);
            }
        }, 300);
    }, 3000);
}

// Helper function to get CSRF token
function getCookie(name) {
    let cookieValue = null;
    if (document.cookie && document.cookie !== '') {
        const cookies = document.cookie.split(';');
        for (let i = 0; i < cookies.length; i++) {
            const cookie = cookies[i].trim();
            if (cookie.substring(0, name.length + 1) === (name + '=')) {
                cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
                break;
            }
        }
    }
    return cookieValue;
}

// Event Listeners
document.addEventListener('DOMContentLoaded', function() {
    // Close modals with Escape key
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') {
            if (!document.getElementById('imageModal').classList.contains('hidden')) {
                closeImageModal();
            }
            if (!document.getElementById('cameraModal').classList.contains('hidden')) {
                closeCameraModal();
            }
        }
    });

    // Pre-verify all image URLs
    document.querySelectorAll('.element-image[src]').forEach(img => {
        if (img.src && img.src !== '') {
            const testImg = new Image();
            testImg.onerror = function() {
                img.style.display = 'none';
                const fallback = img.nextElementSibling;
                if (fallback) {
                    fallback.style.display = 'flex';
                }
            };
            testImg.src = img.src;
        }
    });

    // Handle orientation change for camera
    window.addEventListener('orientationchange', function() {
        if (stream) {
            // Restart camera on orientation change for better compatibility
            setTimeout(() => {
                stopCamera();
                startCamera();
            }, 300);
        }
    });
});
</script>

<style>
    /* Custom scrollbar */
    .overflow-x-auto::-webkit-scrollbar,
    .overflow-y-auto::-webkit-scrollbar {
        width: 6px;
        height: 6px;
    }
    .overflow-x-auto::-webkit-scrollbar-track,
    .overflow-y-auto::-webkit-scrollbar-track {
        background: #f1f1f1;
        border-radius: 4px;
    }
    .overflow-x-auto::-webkit-scrollbar-thumb,
    .overflow-y-auto::-webkit-scrollbar-thumb {
        background: #cbd5e0;
        border-radius: 4px;
    }
    .overflow-x-auto::-webkit-scrollbar-thumb:hover,
    .overflow-y-auto::-webkit-scrollbar-thumb:hover {
        background: #a0aec0;
    }
    
    /* Animations */
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    @keyframes fadeOut {
        from { opacity: 1; transform: translateY(0); }
        to { opacity: 0; transform: translateY(-10px); }
    }
    
    .animate-fade-in {
        animation: fadeIn 0.3s ease-out;
    }
    
    .animate-fade-out {
        animation: fadeOut 0.3s ease-in;
    }
    
    /* Camera feed styling */
    .camera-container {
        position: relative;
        min-height: 300px;
        max-height: 70vh;
    }
    
    #cameraFeed, #imagePreview {
        width: 100%;
        height: auto;
        max-height: 70vh;
        object-fit: contain;
    }
    
    /* Image hover effects */
    .element-image {
        transition: transform 0.2s, box-shadow 0.2s;
    }
    .element-image:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }
    
    /* Modal improvements */
    #imageModal, #cameraModal {
        backdrop-filter: blur(8px);
    }
    
    /* Responsive design */
    @media (max-width: 640px) {
        .camera-container {
            min-height: 250px;
        }
        
        #cameraFeed, #imagePreview {
            max-height: 50vh;
        }
        
        .modal-content {
            margin: 0.5rem;
        }
    }
    
    /* Loading states */
    button:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }
    
    /* Smooth transitions */
    .transition-all {
        transition-property: all;
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
        transition-duration: 200ms;
    }
    
    /* Focus styles for accessibility */
    button:focus, a:focus {
        outline: 2px solid #3b82f6;
        outline-offset: 2px;
    }
</style>
{% endblock %}